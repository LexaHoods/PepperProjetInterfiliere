Pour exécuter ce programme vous devez le compiler avec CMake (voir le tutoriel dans le rapport, ne pas compiler en Unicode sous VS).

La version déjà compilée (avec le fichier .exe) se trouve dans l'archive.
